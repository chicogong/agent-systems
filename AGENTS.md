# 图解 Agent 系统：协作约定

本仓库是公开指南的筹备稿。以 `README.md` 为读者入口，以 `docs/roadmap.md` 为当前范围；不要把私人阅读库、聊天记录或账户数据复制进来。

- 概念图与项目实现图分开。项目事实必须落到固定 commit 的源码、官方文档或可复现运行记录；将事实、推断和未知明确区分。
- 一张图只回答一个主要问题。`.excalidraw` 是图源；SVG、PNG 是从该图源导出的产物。每张图须有等价的文字说明。
- 图稿遵循 `figures/STYLE.md`；Normal 标题与 Code 节点的本地导出是发布视觉基准，不声称 MCP 画布能保留这套字体。
- 用 `excalidraw-agent` 绘图时，核对本地 PNG/SVG；通过 MCP 展示时，还须检查实际交互画布。MCP 检查点不是仓库文件。
- 系统图优先通过 `scripts/build_pi_figures.py` 一类小型生成器维护；改图后同步更新 `.excalidraw`、SVG、PNG、文字版和来源定位，不能只改一份展示图。
- 不用 Star 数或项目名替代选题理由。每个新增系统应提供一种尚未讲清的架构取舍。
- 不把源码静态阅读说成运行验证；不把测试通过说成用户验收。
- 在公开发布前，检查所有图片、代码、引文和依赖的许可，并确定本仓库的内容及代码许可。
